# HailoRT Python package

This directory contains the HailoRT 4.23.0 CPython 3.11 aarch64 wheel used by
the Docker build. Its major/minor version must match the Hailo-8 host driver,
firmware, and mounted `libhailort.so`.
